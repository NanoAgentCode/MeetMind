# 第一阶段功能说明

## 完成范围

1. 上传 MP3、WAV、M4A、WEBM、MP4、MPEG、OGG 录音，校验类型、空文件和大小。
2. 调用可配置 ASR 适配器生成转写文本。
3. 使用 LangGraph 编排纪要生成，输出摘要、关键要点、决策和行动事项。
4. 在同一工作区对生成内容进行人工修改并保存定稿。
5. 将定稿导出为 Word 或 Markdown 文件。
6. 在会议记录页面查看、搜索、筛选、继续处理或删除历史会议。

## 数据存储

会议标题、状态、创建时间、转写文本和结构化纪要存储在 SQLite 的 `meetings` 表中。数据库文件默认位于 `backend/data/meetmind.db`，可通过 `DATABASE_PATH` 修改。

RustFS 仅保存录音和导出文件：

```text
huizhi-meetings/
├── recordings/{meeting_id}/source.{ext}
└── exports/{meeting_id}/minutes.{docx|md}
```

后端通过 RustFS 的 S3 兼容接口访问对象，强制使用 path-style addressing。首次写入时自动创建存储桶。

## 模型协议

纪要生成支持以下协议，并统一转换为相同的结构化纪要：

| `LLM_PROVIDER` | 客户端协议 | 关键配置 |
|---|---|---|
| `openai` | OpenAI | `OPENAI_API_KEY`、`OPENAI_BASE_URL` |
| `anthropic` | Anthropic Claude | `ANTHROPIC_API_KEY`、`ANTHROPIC_BASE_URL` |
| `ollama` | Ollama | `OLLAMA_BASE_URL` |
| `vllm` | OpenAI-compatible | `VLLM_BASE_URL`、`VLLM_API_KEY` |
| `demo` | 本地确定性提取 | 无需外部服务 |

ASR 与 LLM 配置相互独立。Claude、Ollama 和 vLLM 在本系统中只负责纪要生成，不会自动获得语音转写能力。

## 配置分组

后端通过 `settings.<group>.<field>` 访问配置：

| 分组 | 内容 | 示例 |
|---|---|---|
| `app` | 应用限制 | `settings.app.max_upload_mb` |
| `asr` | 转写后端与模型 | `settings.asr.backend` |
| `llm` | 纪要模型协议与模型名 | `settings.llm.provider` |
| `openai`、`anthropic`、`ollama`、`vllm` | Provider 连接参数 | `settings.vllm.base_url` |
| `database` | SQLite 文件路径 | `settings.database.path` |
| `rustfs` | 对象存储连接参数 | `settings.rustfs.bucket` |

环境变量继续使用 `MAX_UPLOAD_MB`、`DATABASE_PATH`、`ASR_*`、`LLM_*`、`OPENAI_*`、`ANTHROPIC_*`、`OLLAMA_*`、`VLLM_*` 和 `RUSTFS_*`。

后端通过 `uv run python backend\main.py` 启动；监听地址和端口分别由 `APP_HOST`、`APP_PORT` 控制。

## 状态流转

```text
uploaded → transcribed → generated → edited → export
```

只有保存人工定稿后才允许导出，避免将未经确认的 AI 内容当作正式纪要。

## 接口

| 方法 | 路径 | 用途 |
|---|---|---|
| POST | `/api/meetings` | 上传录音并创建会议 |
| GET | `/api/meetings` | 按创建时间倒序获取会议记录 |
| GET | `/api/meetings/{id}` | 获取会议详情 |
| DELETE | `/api/meetings/{id}` | 删除会议及关联文件 |
| POST | `/api/meetings/{id}/transcribe` | 执行语音转写 |
| POST | `/api/meetings/{id}/minutes/generate` | 生成结构化纪要 |
| PUT | `/api/meetings/{id}/minutes` | 保存人工定稿 |
| GET | `/api/meetings/{id}/export?format=docx|md` | 导出定稿 |
